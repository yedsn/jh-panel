## 1. 后端连接快照采集

- [x] 1.1 在 `ha_manager_local/index.py` 增加本机 OpenResty 状态页配置发现逻辑，定位包含 `/nginx_status` 和 `stub_status` 的本机 server，并提取实际监听端口。
- [x] 1.2 实现带短超时的本机状态页请求和基于字段名的严格解析，返回 `available`、`active`、`reading`、`writing`、`waiting`、`collected_at` 及不可用原因。
- [x] 1.3 覆盖 OpenResty 未运行、端口不是 80、端口无法发现、状态页不可访问、普通 HTML 响应和截断响应等降级场景，确保异常不抛出到页面接口。
- [x] 1.4 将连接快照纳入 `get_state` 返回数据，并确认已有健康检查、云监控状态和状态文件写入行为不受影响。

## 2. 关闭服务接口与日志

- [x] 2.1 调整 `close_external_service`，在执行停止 OpenResty 前重新采集连接快照，并将实际执行前快照随接口结果返回。
- [x] 2.2 将关闭前连接快照或不可用原因写入本地操作日志，保留现有停止、禁用、mask 和关闭结果语义。
- [x] 2.3 验证连接快照采集失败时仍会继续关闭 OpenResty，并在成功和失败结果中提供可诊断信息。

## 3. 总览与关闭确认交互

- [x] 3.1 在 `ha_manager_local/js/ha_manager_local.js` 扩展初始状态和状态合并逻辑，兼容缺少连接快照字段的旧后端响应。
- [x] 3.2 在总览对外服务区域展示当前活动连接、Reading、Writing、Waiting 和采集时间，并明确提示这是连接快照而非真实用户人数。
- [x] 3.3 为连接快照不可用状态设计降级展示，保留 OpenResty 已运行或已停止的服务状态。
- [x] 3.4 将关闭按钮改为先获取最新连接快照并展示风险确认弹窗，确认前不得调用关闭服务接口；取消或请求失败时不得停止 OpenResty。
- [x] 3.5 在关闭完成提示中展示实际关闭前活动连接数或不可用原因，并刷新总览状态。
- [x] 3.6 补充必要的 `index.html` 样式，确保连接统计在现有桌面和窄屏布局中不发生文字重叠或布局抖动。
- [x] 3.7 在连接详情中说明 HTTP keep-alive 可能暂时保留已完成请求的 TCP 连接，并展示动态读取的 OpenResty 空闲超时时间。
- [x] 3.8 将总览基础状态和连接快照分离加载，基础状态完成后显示总览并异步回填连接统计。
- [x] 3.9 将总览自检从基础状态加载中拆出，异步回填自检结果。
- [x] 3.10 在基础状态和流程配置加载期间展示与总览布局对应的骨架屏。
- [x] 3.11 递归读取 OpenResty `include` 配置发现状态页端口，兼容 `stub_status;` 写法并将网络错误转换为中文摘要；未发现状态页配置时探测 OpenResty 实际监听端口。
- [x] 3.12 对启用 `proxy_protocol` 的状态页候选端口使用 PROXY protocol v1 头重试采集。

## 4. 兼容性与回归验证

- [x] 4.1 对修改后的 Python 文件运行语法检查，并验证 `status` 和 `get_state` 接口在 OpenResty 运行与停止状态下均能返回。
- [x] 4.2 在状态页监听 80 和非 80 端口两种配置下验证连接快照数值解析，确认不依赖固定端口和固定 token 下标。
- [x] 4.3 验证状态页返回错误页面、不可访问和 OpenResty 已停止时，总览显示不可用提示且关闭流程仍可确认执行。
- [x] 4.4 验证有连接、零连接、取消确认、确认关闭、关闭失败五种交互场景，并确认日志包含实际关闭前快照。
- [x] 4.5 回归验证主备切换流程、OpenResty 自检、云监控状态上报和浏览器标题刷新未被连接快照功能破坏。
- [x] 4.6 验证 `keepalive_timeout` 可正确解析并在连接详情中展示，配置无法读取时保留未知状态。
- [x] 4.7 验证连接快照尚未返回时总览已完成首次渲染，返回后可正确回填统计数据。
- [x] 4.8 验证基础状态、自检和连接快照分别加载，且自检结果返回后正确更新总览。
- [x] 4.9 验证基础状态尚未返回时展示骨架屏，基础数据完成后再加载实际总览。
- [x] 4.10 验证状态页配置位于 `include` 文件、`stub_status;` 写法、端口提前关闭、实际监听端口探测和未配置状态页时的降级提示。
- [x] 4.11 验证启用 `proxy_protocol` 的状态页端口可通过 PROXY protocol 重试采集。
