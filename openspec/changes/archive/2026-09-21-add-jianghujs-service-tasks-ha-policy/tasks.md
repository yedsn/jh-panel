## 1. JianghuJS 项目数据与任务执行入口

- [x] 1.1 为 JianghuJS 项目记录增加兼容的 `log_clean_enabled` 读取和保存逻辑，并确保已有项目记录缺少该字段时可正常展示和编辑。
- [x] 1.2 实现项目预备执行入口：动态读取全部登记项目，逐个校验目录和根目录 `package-lock.json`，顺序执行 `npm ci` 并输出可审计的项目级结果。
- [x] 1.3 实现统一日志清理执行入口：读取开启 `log_clean_enabled` 的项目，校验 `logs` 目录，使用统一规则调用现有清理能力并输出项目级结果。
- [x] 1.4 在 JianghuJS 后端命令分发中注册两个任务执行入口，并确保命令可从计划任务环境稳定运行。

## 2. JianghuJS 服务配置界面

- [x] 2.1 在 JianghuJS 插件导航中增加“服务配置”入口，并更新脚本资源版本避免浏览器缓存旧页面。
- [x] 2.2 实现 `JianghuJS管理器项目预备` 的读取、创建、修改和删除界面，支持每天和每 N 分钟周期。
- [x] 2.3 实现 `JianghuJS管理器日志清理` 的读取、创建、修改和删除界面，支持每天和每 N 分钟周期及统一保留规则。
- [x] 2.4 调整项目新增、导入和编辑界面：仅保留“自动清理日志”参与开关，移除每项目的清理时间、保留规则和单项目任务配置。
- [x] 2.5 在项目列表或编辑状态中正确回显项目日志清理参与状态，且项目开关变更不创建或删除单项目计划任务。

## 3. 旧日志清理任务迁移

- [x] 3.1 识别旧格式 `[勿删]项目[<项目名>]日志清理` 任务，并将可可靠匹配的项目按旧任务启用状态保存到 `log_clean_enabled`。
- [x] 3.2 将旧任务删除操作放在统一日志清理任务创建成功之后；创建失败时保留全部旧任务。
- [x] 3.3 对无法匹配项目或无法迁移的旧任务保留原任务，并在服务配置操作结果中提供明确提示。

## 4. 本地主备任务策略与自检

- [x] 4.1 在 `ha_manager_local` 中增加 `JianghuJS管理器项目预备` 的计划任务常量、主备自检定义和实际状态展示。
- [x] 4.2 在切换为备用机的流程配置和动作映射中增加启用项目预备任务的步骤及修复说明。
- [x] 4.3 在切换为主机的流程配置和动作映射中增加停用项目预备任务的步骤及修复说明。
- [x] 4.4 验证项目预备任务不存在时，主备自检显示提醒且切换流程不会因此阻断。

## 5. 验证与回归

- [x] 5.1 对修改后的 Python 插件入口运行语法检查，并确认 JianghuJS 和本地主备插件的 `status` 命令仍能快速返回。
- [x] 5.2 手动验证项目预备任务的创建、修改、删除及运行，覆盖正常项目、目录不存在和缺少 `package-lock.json` 的项目。
- [x] 5.3 手动验证统一日志清理任务的创建、修改、删除及运行，覆盖开启/关闭项目、缺失日志目录和统一保留规则。
- [x] 5.4 验证旧单项目日志清理任务的成功迁移、统一任务创建失败时不删除旧任务、无法匹配旧任务时保留并提示三种场景。
- [x] 5.5 验证主机和备用机自检结果，以及两种角色切换流程对 `JianghuJS管理器项目预备` 的开关行为。
