## 1. 后端批量操作基础

- [x] 1.1 改造 `nfs-util` 参数解析，使其稳定读取面板传入的 JSON 参数，并兼容现有调用格式。
- [x] 1.2 增加批量 ID 解析与去重辅助逻辑，校验空选择、重复 ID、不存在记录和不支持的动作。
- [x] 1.3 抽取单项挂载与卸载脚本构建辅助函数，对服务器、共享路径和本地路径使用标准 shell 参数转义。
- [x] 1.4 增加基于精确目标路径的运行时挂载状态检查，区分正确挂载、未挂载和目标冲突，并供单项与批量逻辑复用。

## 2. 批量挂载与卸载接口

- [x] 2.1 实现批量挂载/卸载脚本生成入口，按请求顺序为每个有效 ID 输出条目名称、开始信息和成功、跳过或失败结果。
- [x] 2.2 确保组合脚本中单个条目失败后继续执行其余条目，且卸载失败时不使用强制或延迟卸载参数。
- [x] 2.3 让现有单项挂载和卸载入口复用新的脚本构建逻辑，保持原有前端调用和日志体验兼容。
- [x] 2.4 在插件命令分发中注册批量脚本入口，并验证非法动作不会生成或执行命令。

## 3. 批量自动挂载接口

- [x] 3.1 抽取规范 `/etc/fstab` 配置行和显式 enable/disable 设置逻辑，保证开启不重复追加、关闭只删除完全匹配行。
- [x] 3.2 实现批量自动挂载入口，一次读取配置并处理全部有效 ID，返回成功、跳过、失败和不存在记录的摘要。
- [x] 3.3 使用同目录临时文件和原子替换提交 `/etc/fstab` 变更，保留原文件权限、无关配置、注释和空行，并处理写入失败。
- [x] 3.4 让现有单项自动挂载切换复用显式设置辅助逻辑，并在命令分发中注册批量自动挂载入口。

## 4. 挂载列表多选交互

- [x] 4.1 在 `nfs-util` 挂载列表增加表头全选框和逐行复选框，并使用 `.nfs-util-panel` 作用域内的选择器维护已选 ID。
- [x] 4.2 实现选择、取消选择、全选和取消全选状态同步，至少选择一项时显示四个批量操作按钮，无选择时隐藏。
- [x] 4.3 列表刷新后保留仍存在的选择、移除失效 ID，并正确同步表头全选状态。
- [x] 4.4 增加批量挂载和卸载前端流程：批量卸载先显示包含已选数量的确认，确认后调用批量脚本入口并复用日志窗口执行。
- [x] 4.5 增加批量开启和取消自动挂载流程，两类操作均在确认后调用显式批量接口，并展示处理摘要。
- [x] 4.6 批量操作完成后刷新列表展示实际状态，同时保持单项挂载、卸载、编辑、删除和自动挂载操作可用。
- [x] 4.7 为 `nfs-util.js` 加载地址增加并更新资源版本参数，避免浏览器缓存旧脚本。

## 5. 验证与回归

- [x] 5.1 对 `plugins/nfs-util/index.py` 运行 Python 语法检查，并确认 `status` 命令快速返回 `start`。
- [x] 5.2 手动验证选择一项、多项、全选、取消全选和刷新后的选择保留，确认批量区域不包含批量删除。
- [x] 5.3 手动验证批量挂载覆盖全部成功、已挂载跳过、目标冲突、NFS 不可达和不存在 ID 场景，并确认后续条目继续执行。
- [x] 5.4 手动验证批量卸载覆盖全部成功、未挂载跳过、目录占用失败、用户取消确认和不存在 ID 场景。
- [x] 5.5 手动验证混合状态下批量开启/取消自动挂载及重复执行，确认 `/etc/fstab` 无重复行且无关内容与权限保持不变。
- [x] 5.6 回归验证单项挂载、卸载、自动挂载切换、添加、编辑和删除流程，并确认列表最终状态与系统实际状态一致。
