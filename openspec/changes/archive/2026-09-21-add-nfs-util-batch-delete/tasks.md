## 1. 后端批量删除

- [x] 1.1 增加挂载记录批量删除辅助逻辑，在单个数据库写会话中删除去重后的有效 ID，并返回已删除和不存在摘要。
- [x] 1.2 增加 `mount_batch_delete` 插件入口，校验空选择并注册命令分发。

## 2. 前端批量删除

- [x] 2.1 在已有批量操作区增加“批量删除”按钮，并复用当前选择状态。
- [x] 2.2 使用 `safeMessage` 展示选择数量和已转义的挂载名称，明确说明不会卸载或清理自动挂载。
- [x] 2.3 调用批量删除接口、展示删除与跳过摘要、清理已删除选择并刷新列表。
- [x] 2.4 更新 `nfs-util.js` 资源版本参数。

## 3. 验证与回归

- [x] 3.1 扩充后端测试，覆盖 ID 去重、部分不存在、空选择和仅删除管理记录。
- [x] 3.2 扩充前端测试，覆盖按钮存在、确认取消、确认文案和删除后刷新。
- [x] 3.3 运行 Python/JavaScript 语法检查、NFS 批量操作测试、OpenSpec 严格校验和差异检查。
