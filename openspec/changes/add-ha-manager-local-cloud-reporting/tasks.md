## 1. 云监控数据模型与安全基础

- [x] 1.1 在云监控 SQLite 初始化/迁移逻辑中幂等创建 `ha_pair`、`ha_host_state`、`ha_switch_task` 及其唯一索引，并包含主机当前 `switch_task_id` 字段。
- [x] 1.2 创建受限权限的 `/www/server/jh-monitor/logs/ha_switch/` 目录，并实现安全的任务 ID 到日志文件、序号索引和任务锁路径映射。
- [x] 1.3 实现主备关系和机器状态的枚举校验。
- [x] 1.4 实现当前机器快照到 `ha_pair.status` 和 `status_text` 的单机状态计算，并覆盖等待上报、切换中、失败/离线、自检异常和正常角色优先级。

## 2. 云监控单机上报 API

- [x] 2.1 实现 `POST /ha/api/local/pair/create`，填写关系名称并生成唯一 `pair_id`，创建等待上报主备关系。
- [x] 2.1b 实现 `POST /ha/api/local/pair/delete`，删除主备关系及关联机器状态、任务摘要和任务日志文件。
- [x] 2.2 实现 `POST /ha/api/local/register`，验证已存在的主备关系后按 `pair_id + host_id` 幂等写入当前机器身份与初始状态。
- [x] 2.3 实现 `POST /ha/api/local/report` 的状态快照校验与事务性主机、可选任务摘要更新，拒绝跨关系或跨主机任务。
- [x] 2.4 实现任务日志处理：在任务锁内按 `seq` 排序追加未接收日志，原子更新侧车序号索引，并容忍重复及乱序重试。
- [x] 2.5 实现 `GET /ha/api/local/switch-log`，严格验证任务和非负偏移量，仅返回 HA 日志目录内的增量内容和下一字节偏移量。

## 3. 云监控只读展示

- [x] 3.1 增加主备关系创建和列表视图，展示主备关系 ID、当前机器、角色、自检、切换摘要、状态说明和最近上报时间。
- [x] 3.2 增加任务详情视图及增量日志轮询，使用日志读取 API 追加显示任务日志。
- [x] 3.3 确认单机页面和接口不包含备用机、双机一致性、角色切换或任务控制入口。

## 4. 本地版插件上报集成

- [x] 4.1 扩展 `ha_manager_local` 运行配置，增加上报队列、日志序号持久化字段，普通页面状态和响应仅展示主备关系 ID。
- [x] 4.2 在绑定信息或本机身份改变后调用注册接口；缺少主备关系 ID 时返回明确的未配置状态而不伪造成功。
- [x] 4.3 实现可重试的 HTTP 注册和报告客户端，按 `report_interval` 发送本机状态快照，并把失败原因及待重试状态保存在本机运行目录。
- [x] 4.4 从本机切换状态和步骤日志构造任务摘要、递增日志序号及待发送日志增量，在任务创建、开始、完成、失败和恢复后触发上报。
- [x] 4.5 保证云监控请求失败不会阻塞、回滚或伪造本机步骤执行结果，并在后续报告时重试未确认的状态和日志。

## 5. 验证

- [x] 5.1 为主备关系创建、ID 注册、无任务心跳、无效枚举及跨关系任务拒绝添加云监控 API 测试。
- [x] 5.2 为单机状态优先级添加测试，覆盖等待上报、切换中、失败、离线、自检 warning/danger 及正常 master/standby。
- [x] 5.3 为日志幂等与读取添加测试，覆盖重复 `seq`、乱序日志、非法任务/偏移量和连续偏移读取。
- [x] 5.4 在单机环境端到端验证：添加主备关系、填写关系 ID、注册、周期上报、运行中/失败切换任务与增量日志展示。
- [x] 5.5 确认本地版本机切换流程可用，且云监控 HA 模块不依赖 SSH 版协议或 SSH 版界面。
