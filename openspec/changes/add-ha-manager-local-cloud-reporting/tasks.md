## 1. 云监控数据模型与安全基础

- [ ] 1.1 在云监控 SQLite 初始化/迁移逻辑中幂等创建 `ha_pair`、`ha_host_state`、`ha_switch_task` 及其唯一索引，并包含主机当前 `switch_task_id` 字段。
- [ ] 1.2 创建受限权限的 `/www/server/jh-monitor/logs/ha_switch/` 目录，并实现安全的任务 ID 到日志文件、序号索引和任务锁路径映射。
- [ ] 1.3 实现归属和机器状态的枚举校验、密钥安全比较及密钥脱敏日志辅助函数。
- [ ] 1.4 实现当前机器快照到 `ha_pair.status` 和 `status_text` 的单机状态计算，并覆盖等待上报、切换中、失败/离线、自检异常和正常角色优先级。

## 2. 云监控单机上报 API

- [ ] 2.1 实现 `POST /ha/api/local/pair/create`，校验唯一 `pair_id`、生成高熵密钥、创建等待上报归属，并仅在创建响应中返回密钥。
- [ ] 2.2 实现 `POST /ha/api/local/register`，验证归属和密钥后按 `pair_id + host_id` 幂等写入当前机器身份与初始状态。
- [ ] 2.3 实现 `POST /ha/api/local/report` 的状态快照校验与事务性主机、可选任务摘要更新，拒绝跨归属或跨主机任务。
- [ ] 2.4 实现任务日志处理：在任务锁内按 `seq` 排序追加未接收日志，原子更新侧车序号索引，并容忍重复及乱序重试。
- [ ] 2.5 实现 `GET /ha/api/local/switch-log`，严格验证任务和非负偏移量，仅返回 HA 日志目录内的增量内容和下一字节偏移量。

## 3. 云监控只读展示

- [ ] 3.1 增加单机归属创建和列表视图，展示归属、当前机器、角色、自检、切换摘要、状态说明和最近上报时间。
- [ ] 3.2 增加任务详情视图及增量日志轮询，使用日志读取 API 追加显示任务日志。
- [ ] 3.3 确认单机页面和接口不包含备用机、双机一致性、角色切换或任务控制入口。

## 4. 本地版插件上报集成

- [ ] 4.1 扩展 `ha_manager_local` 运行配置，增加仅后端可写/可读的 `api_secret` 和上报队列、日志序号持久化字段，确保普通页面状态和响应不会回显密钥。
- [ ] 4.2 增加后端专用的受信密钥配置入口，并在绑定信息、密钥或本机身份改变后调用注册接口；缺少密钥时返回明确的未配置状态而不伪造成功。
- [ ] 4.3 实现可重试的 HTTP 注册和报告客户端，按 `report_interval` 发送本机状态快照，并把失败原因及待重试状态保存在本机运行目录。
- [ ] 4.4 从本机切换状态和步骤日志构造任务摘要、递增日志序号及待发送日志增量，在任务创建、开始、完成、失败和恢复后触发上报。
- [ ] 4.5 保证云监控请求失败不会阻塞、回滚或伪造本机步骤执行结果，并在后续报告时重试未确认的状态和日志。

## 5. 验证

- [ ] 5.1 为归属创建、认证注册、无任务心跳、无效枚举及跨归属任务拒绝添加云监控 API 测试。
- [ ] 5.2 为单机状态优先级添加测试，覆盖等待上报、切换中、失败、离线、自检 warning/danger 及正常 master/standby。
- [ ] 5.3 为日志幂等与读取添加测试，覆盖重复 `seq`、乱序日志、非法任务/偏移量和连续偏移读取。
- [ ] 5.4 在单机环境端到端验证：创建归属、受信写入密钥、注册、周期上报、运行中/失败切换任务与增量日志展示。
- [ ] 5.5 回归确认 `ha_manager_ssh` 的现有云监控协议和本地版本机切换流程未被本变更影响。
